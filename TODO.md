# TODO — clinical-trial-stats

## 🔗 新会话接续（2026-07-25 交接锚段）

- **状态快照**：产线 w1–w9 已封版——本仓（A）ESM 修复 / 测试 116→327 / 统一参数验证器 / R 对照试点；B 仓（wxapp_device-helper）四模块解锁（paired·multigroup·rateCI·meanCI）/ 审计清单 P0–P2 清 / 测试 202→313
- **下一波 w10 = 本仓 R 对照扩展全量**（活跃组①；McNemar 与等效率终点优先）
- **主控模式触发词**：「使用主控模式」
- **产线账本**：本仓根 `PROGRESS.tsv`（单一账本，两仓共用）
- **产线约定四条**：① 派工显式指定 model；② 算法改动先落本仓（含测试）再由 B 仓 `scripts/sync-statistics.sh` 带回 B；③ 席位禁 commit（主控统一落账）；④ 审计 FAIL 打回 fixer 补修后复审
- **真机回归**（B 仓四模块 UI）：用户明示排后，不阻塞本仓 w10 及后续

---

> 待办单一信源。来源：2026-07-02 停滞恢复审计（`docs/audit/AUDIT_20260702_stagnation.md`，问题编号 CTS-xx 见该报告）+ 停滞前既有排期（STATISTICS_AUDIT_REPORT §九、P0_API_MEMO）。
> 项目停滞于 2026-02-01；已完成条目按原批次归档于 `docs/TODO-ARCHIVE.md`，本文件只留活跃项，按**推进组序**排列。
> 外部引用文档在 B 侧仓 `~/Repos/wxapp_device-helper/docs/` 下：`VALIDATION_STRATEGY.md`、`P0_API_MEMO.md`、`STATISTICAL_METHODS_EXPANSION_SPEC.md`、`test-framework/`（811 快照用例）——四处路径 2026-07-10 均已实证存在。
> 最后更新：2026-07-25（w10 交接整理：15 条已完成条目搬移入 `docs/TODO-ARCHIVE.md`，活跃 10 条按「R 对照扩展 / 快照搬迁 / P1P2 / 功能扩展 / npm 发布与双源终态」重排，并加本页头部接续锚段）。
> 沿革注记（2026-07-10 两轮核对）：① 状态核对——b2cb7ad 后无新提交、工作区干净、npm registry 实测 404 未发布；② 逐条实证核查——24 个条目逐条对照代码/文档/环境实况，全部确认未完成，无一"其实已经做了"；核查发现的修复定位细节已回注 CTS-01/03/04 条目（现已随完成搬入归档件）。

---

## ① R 对照验证扩展全量【新会话首选 = w10】

- [ ] 扩展全量：按 README 进度表逐行补"对照验证"列
  **推进思路**：产出物固定为 `tests/fixtures/golden-<module>.json`（每个值注明 R 命令），让对照验证成为可重跑的资产而不是一次性报告；McNemar 与等效率终点这两个本次审计"未验证"项优先排入
  **w10 派工标尺（2026-07-25 交接补注）**：模板 = w9 已封版试点链——fixture 目录 `tests/fixtures/r/` + golden 消费测试 + `npm run golden:two-group-ni` script 形态，逐模块照此形态复制；优先序按账面既定：McNemar → 等效率终点 → README 进度表其余行

## ② 广度回归资产搬迁

- [ ] wxapp 侧的 docs/test-framework 811 快照用例可搬来做广度回归（该资产只在 B 侧）

## ③ 算法层 P1/P2 与 §九表回更（原第三批遗留）

- [ ] P1：非中心 t 分布、FM/MN 设计阶段路径、连续性校正选项、RR/OR 效应量尺度
- [ ] P2：精确二项功效、logGamma、Brent 求解器、精度驱动样本量、反正弦变换
- [ ] 顺手：§九表"反向功效 P0 ❌"改 ✅（01-31 已实现，表未更新）；DATA_SOURCES 功能矩阵同步
  **推进思路**：P1/P2 项在 R 对照验证（下一批）跑通之前不急——对照验证会客观暴露哪些近似真的不够用，避免凭直觉排优先级
  **排序依据（2026-07-25 交接补注）**：w9 试点已产出已知偏差清单——`tests/fixtures/golden-two-group-ni.json` 的 `known_deviation` 条目（2 条超容差，近似薄弱区）可直接作 P1 精度项的排序依据

## ④ 功能扩展与发布运营（原第五批，停滞前的原定方向）

- [ ] **Log-rank 生存分析**（P0_API_MEMO 排的下一个模块；wxapp 的 STATISTICAL_METHODS_EXPANSION_SPEC §4.2 有 5 天量级的需求稿）
- [ ] McNemar 独立模块 / Kappa / AUC 比较（同 spec 第二批）
- [ ] **JOSS 投稿**（P0_API_MEMO 的目标；前置 = 第一、二、四批全部完成——JOSS 审稿会查测试、CI、安装可用性，正是目前的短板清单）
- [ ] CTS-10/11/13~18 规范收尾（错误处理统一、API 风格收敛、normalCDF 精度、陈旧元数据清理等，见审计报告 P2 表）

## ⑤ npm 发布重拍板 / 双源终态（与 wxapp_device-helper 共同决策，两仓 TODO 互为镜像）

> **待用户重新拍板（2026-07-25 交接提出）**：CTS-08 于 2026-07-24 拍的是"npm 暂不发布、先 GitHub"（见 `docs/TODO-ARCHIVE.md` 第一批），当时的短板 = 无 CI、无入口校验、无对照验证。此后 CI + 统一参数验证器（w8）+ R 对照试点（w9）均已落地，发布条件已变，**npm publish 可重议**——这也是下条「终态 npm 依赖化」的前置。
> 镜像核对注（2026-07-10）：B 侧 `wxapp_device-helper/TODO.md` 同名段存在（B 侧拆 3 条，本仓打包 1 条），实质决策一致、无矛盾漂移；B 侧 WX-13a 声明 statistics 测试写在本仓侧（对应 CTS-05a），两仓互认。

- [ ] **终态 npm 依赖化**：B 删除 `src/utils/statistics/` 改依赖本包（前置：CTS-08 发布决策落地）

---

## 已完成批次索引（原批次结构，条目全文见 `docs/TODO-ARCHIVE.md`）

- **第一批 发布阻断修复**：✅ 全部完成 2026-07-24（5 条：CTS-01/06/07/08/09）
- **第二批 算法层 P1 修复**：✅ 全部完成 2026-07-24/25（6 条：CTS-02/03/04/05a/05b/12）
- **第三批 停滞前既有排期**：P0 统一参数验证器完成 2026-07-25（1 条）；未完成项 P1/P2/§九表回更见活跃组③
- **第四批 R 对照验证**：装 R 环境 + 两组-率-非劣效试点链完成 2026-07-25（2 条）；未完成项见活跃组① ②
- **第五批 功能扩展与发布运营**：全部未完成，见活跃组④
- **双源同步**：过渡期同步机制完成 2026-07-24（1 条）；未完成项见活跃组⑤
